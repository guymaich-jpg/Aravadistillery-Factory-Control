{
  "name": "aravadistillery-backend",
  "version": "1.0.0",
  "description": "Shared Vercel serverless backend for Arava Distillery apps (Factory Control + CRM)",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit && node scripts/build-public.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^3.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.4.0"
  }
}
